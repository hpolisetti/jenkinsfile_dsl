pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh "set brf = ${env.santhosh} && echo $brf"
            }
        }
    }
}
